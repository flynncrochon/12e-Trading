# resources/

Build-time resources picked up by electron-builder (`directories.buildResources`).

- `icon.png` — placeholder, replace with a real 512x512 (or higher) PNG before
  shipping. electron-builder auto-derives platform-specific icons from this.
- Future: `entitlements.mac.plist`, `installer.nsh`, default `config.json`.
